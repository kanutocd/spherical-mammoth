Spherical Mammoth is installed in namespace {{ .Release.Namespace }}.

Check workload readiness:

  kubectl --namespace {{ .Release.Namespace }} get pods

For local access, follow docs/runbooks/local-kubernetes.md or run:

  make kind-verify
