# Cloud SQL PostgreSQL module

Creates a private PostgreSQL 17 instance with backups, PITR, query insights, and
logical decoding. Database users, grants, and publications belong to the
post-provisioning database bootstrap.
