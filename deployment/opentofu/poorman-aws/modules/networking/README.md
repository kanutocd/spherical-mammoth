# Sealed networking

One VPC and one private subnet, deliberately without an internet gateway, public
IPv4 address, NAT gateway, or paid interface endpoints. Operators enter through
EICE. The instance reaches S3 through the gateway endpoint.
