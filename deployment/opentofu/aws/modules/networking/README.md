# Networking module

Creates a DNS-enabled VPC, public and private subnets across multiple
availability zones, an internet gateway, and either one cost-conscious NAT
gateway or one NAT gateway per AZ.
